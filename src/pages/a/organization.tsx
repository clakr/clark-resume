import type { NextPage } from "next";
import Admin from "../../components/Admin";

const Organization: NextPage = () => {
  return <Admin pageTitle="Organization"></Admin>;
};

export default Organization;
